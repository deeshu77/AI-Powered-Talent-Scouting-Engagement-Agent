# Environment variables
python-dotenv

# HTTP requests (Groq API calls in agent.py)
requests

# Embeddings — no torch/transformers, lightweight (~50MB)
fastembed

# Vector search — pure Python, works on Streamlit Cloud without issues
chromadb

# Frontend
streamlit
pdfplumber
python-docx